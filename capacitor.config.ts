import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Admin.DrShop2City-FrontEnd',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
